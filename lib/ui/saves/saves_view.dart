import 'package:flutter/material.dart';

class SavesView extends StatelessWidget {
  const SavesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
